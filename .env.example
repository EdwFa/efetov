# Скопируйте в .env на сервере и подставьте свои значения.
# .env в git не входит.

EFETOV_JWT_SECRET=смените-на-длинный-случайный-секрет
EFETOV_CORS_ORIGINS=https://example.com
# Каталог SQLite (по умолчанию backend/data)
# EFETOV_DATA_DIR=/var/lib/efetov
